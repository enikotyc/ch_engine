import React, { Component } from 'react';
import Button from 'react-bootstrap/Button';

export default class Buttons extends Component{
    render(){
        return(
            <div id="engineA">
                <Button variant = "primary">Engine</Button>
            </div>
            
        )
    }
}